import React, { FC } from 'react';
type RegisterProps = {
  setActiveState: (route: string) => void;
};

const Register: FC<RegisterProps> = ({ setActiveState }) => {
  return <div>Register</div>;
};

export default Register;
